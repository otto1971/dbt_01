version: 2

models:
  - name: src_listings
    description: "dbtd"
    columns:
      - name: listing_id
        tests:
          - not_null
          - unique
      - name: listing_name
          - not_null